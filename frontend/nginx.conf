server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Route all requests to index.html so the React router handles them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
